Utilities and Demos packs

Utilities.unitypackage contains assets that extend the OSA's capabilities, 
but also general purpose utilities that can be used outside OSA

Demos.unitypackage contains all the example scenes and their associated assets. 
It depends on Utilities.unitypackage, so import Utilities.unitypackage first.